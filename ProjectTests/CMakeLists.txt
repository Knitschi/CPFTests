# This is the root CMakeLists file for the project tests
# It includes the project call and then runs calls add_subdirectory() for all the files
# that are specified in the TEST_FILES variable.

project(ProjectTests)


